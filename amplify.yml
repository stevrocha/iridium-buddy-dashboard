version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "no install needed"
    build:
      commands:
        - echo "static site, nothing to build"
  artifacts:
    baseDirectory: /
    files:
      - 'index.html'
      - 'data/**/*'
  cache:
    paths: []
